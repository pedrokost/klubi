
import { asYear } from 'klubi/helpers/as-year';
import { module, test } from 'qunit';

module('Unit | Helper | as year', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = asYear([42]);
    assert.ok(result);
  });
});

