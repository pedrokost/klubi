
import { klubIcon } from 'klubi/helpers/klub-icon';
import { module, test } from 'qunit';

module('Unit | Helper | klub icon', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = klubIcon([42]);
    assert.ok(result);
  });
});

