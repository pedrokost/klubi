
import { intersection } from 'klubi/helpers/intersection';
import { module, test } from 'qunit';

module('Unit | Helper | intersection');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = intersection([42]);
  assert.ok(result);
});

