{{leaflet-map wantedZoom=zoom wantedCenter=markerCenter klubs=model showKlub="showKlub" isShowPage=isShowPage}}

{{data-bar isShowPage=isShowPage klubs=model action="zoomToMarker"}}

{{#unless anyKlub}}
  <div class="loader">
    <div class="spinner-container">
      <div class="spinner"></div>
      <div class="spinner"></div>
      <div class="spinner"></div>
    </div>
  </div>
{{/unless}}

{{outlet}}
